const iconContainer = {
    display: "flex",
    justifyContent: "flex-end"
}

const header = {
    borderRadius: "5px"
}

const logo = {
    fontWeight: "bold",
    cursor: "default"
}

export {
    iconContainer,
    header,
    logo
}
